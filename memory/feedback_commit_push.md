---
name: commit and push after every change
description: User requires committing with descriptive message and pushing to main after every change
type: feedback
---

After every change, commit with a descriptive message and push to main.

**Why:** App is deployed on Vercel — pushing to main triggers a deployment.

**How to apply:** Every time a file is edited or created, immediately run git add + git commit + git push origin main.
