// Shared sitemap constants. URLs-per-chunk stays under the 50k / 50MB sitemap
// limit; the index and the artwork chunks must agree on this value.
export const SITE = 'https://www.publicartcollections.net';
export const CHUNK = 45000;
